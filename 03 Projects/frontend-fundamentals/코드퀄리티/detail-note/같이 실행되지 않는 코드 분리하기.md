---
tags:
  - detail-note
  - 코드퀄리티
aliases:
  - 가독성
  - 맥락 줄이기
---
동시에 실행되지 않는 코드가 하나의 함수 또는 컴포넌트에 있으면 동작을 파악하기 어렵다
구현 부분에 많은 숫자의 분기가 들어가서 어떤 역할을 하는지 이해하기도 어렵다

### 코드 살펴보기
---
다음 `SubmitButton`은 사용자의 권한에 따라 다르게 동작한다
- 사용자 권한이 `viewer` 일 경우 -> 초대 버튼 비활성화, 애니메이션 재생 X
- 사용자 권한이 `일반 권한` -> 초대 버튼 활성화, 애니메이션 재생 O

```tsx
function SubmitButton() {
	const isViewer = useRole() === "viewer"
	
	useEffect(() => {
		is (isViewer) {
			return;
		}
		
		showButtonAnimation();
	}, [isViewer]);
	
	return isViewer ? (
		<TextButton disable>Submit</TextButton>
	) : (
		<Button type="submit">Submit</Button>
	)

}
```

**문제점**: 해당 컴포넌트는 사용자가 가질 수 있는 2가지의 권한 상태를 하나의 컴포넌트 내부에서 한 번에 처리 중이다

### 코드 개선하기
---
```tsx
function SubmitButton() {
  const isViewer = useRole() === "viewer";

  return isViewer ? <ViewerSubmitButton /> : <AdminSubmitButton />;
}

function ViewerSubmitButton() {
  return <TextButton disabled>Submit</TextButton>;
}

function AdminSubmitButton() {
  useEffect(() => {
    showButtonAnimation();
  }, []);

  return <Button type="submit">Submit</Button>;
}
```

사용자 권한에 따라 SubmitButton을 분리하여 처리하도록 하면서 분기가 줄어 들었다
`<ViewerSubmitButton />` 과 `<AdminButton />`이 분리되면서 하나의 분기만 처리하기 때문에, 코드를 읽는 사람이 고려해야 할 사항이 줄어들었다



### 관련
---
https://frontend-fundamentals.com/code-quality/code/examples/submit-button.html